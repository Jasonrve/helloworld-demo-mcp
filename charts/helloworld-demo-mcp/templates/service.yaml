apiVersion: v1
kind: Service
metadata:
  name: {{ include "helloworld-demo-mcp.fullname" . }}
  labels:
    {{- include "helloworld-demo-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "helloworld-demo-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
